/**
 * Impact-CV Themes — DEPRECATED
 * 
 * These themes were not actually used by the rendering engine (TemplatePreview).
 * All visual customization is now handled through the 9 Presets in the Presets tab,
 * which push full settings (colors, fonts, layout, content) to Redux.
 * 
 * Keeping this file for backward compatibility but exporting an empty array.
 */
export const impactCVThemes: never[] = [];
