/**
 * Reactive-Resume templates have been consolidated into the
 * unified template system. Templates are now managed through
 * the backend configurations and Impact-CV themes.
 * 
 * This file is kept for backward compatibility but exports
 * an empty array since all templates are now in the registry.
 */
export const reactiveResumeTemplates: never[] = [];
